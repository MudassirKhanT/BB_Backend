services:
  - type: web
    name: backend
    runtime: node
    plan: free
    buildCommand: npm install && tsc
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 10000